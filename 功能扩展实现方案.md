# 达梦数据库 CLI 工具功能扩展实现方案

## 一、技术栈文档参考

| 技术 | 文档来源 | 关键最佳实践 |
|------|----------|--------------|
| Commander.js | `/tj/commander.js` | 嵌套子命令、独立可执行文件、配置选项继承 |
| readline | Node.js 内置模块 | createInterface、自动补全、历史记录、信号处理 |
| csv-parse/stringify | `/adaltas/node-csv` | 流式管线、同步解析、列名映射 |
| fast-csv | `/c2fo/fast-csv` | 高性能流式、TypeScript 支持 |

---

## 二、命令层级结构设计

```
dm
├── schema              # Schema 管理
│   ├── list            # 列出 Schema
│   ├── create          # 创建 Schema
│   ├── drop            # 删除 Schema
│   ├── use             # 切换 Schema
│   └── objects         # 查看 Schema 对象
├── table               # 表管理
│   ├── list            # 列出表
│   ├── describe        # 查看表结构
│   ├── create          # 创建表（交互式）
│   ├── alter           # 修改表
│   ├── drop            # 删除表
│   ├── data            # 查看表数据
│   ├── index           # 索引管理
│   │   ├── list        # 列出索引
│   │   ├── create      # 创建索引
│   │   └── drop        # 删除索引
│   └── partition       # 分区管理
│       ├── list        # 列出分区
│       └── create      # 创建分区
├── user                # 用户管理
│   ├── list            # 列出用户
│   ├── create          # 创建用户
│   ├── drop            # 删除用户
│   ├── alter           # 修改用户
│   ├── grant           # 授权
│   ├── revoke          # 撤销权限
│   ├── role            # 角色管理
│   │   ├── list        # 列出角色
│   │   ├── create      # 创建角色
│   │   ├── grant       # 授予角色
│   │   └── revoke      # 撤销角色
│   └── show            # 查看权限
├── data                # 数据导入导出
│   ├── import          # 导入数据
│   │   ├── csv         # CSV 导入
│   │   ├── json        # JSON 导入
│   │   └── sql         # SQL 导入
│   ├── export          # 导出数据
│   │   ├── csv         # CSV 导出
│   │   ├── json        # JSON 导出
│   │   └── sql         # SQL 导出（DDL）
│   └── backup          # 数据库备份
├── performance         # 性能分析
│   ├── explain         # 执行计划
│   ├── slow            # 慢查询分析
│   ├── index-suggest   # 索引建议
│   └── stats           # 统计信息
├── maintenance         # 数据库维护
│   ├── tablespace      # 表空间管理
│   │   ├── list        # 列出表空间
│   │   ├── create      # 创建表空间
│   │   ├── drop        # 删除表空间
│   │   └── usage       # 使用情况
│   ├── log             # 日志管理
│   │   ├── list        # 列出日志
│   │   ├── switch      # 切换日志
│   │   └── archive     # 归档日志
│   ├── analyze         # 统计信息更新
│   └── defrag          # 碎片整理
└── shell               # 交互式 Shell
```

---

## 三、核心代码实现

### 3.1 CLI 入口重构 (src/cli.ts)

**Commander.js 嵌套子命令最佳实践**

```typescript
#!/usr/bin/env node
import { Command, InvalidArgumentError } from 'commander';
import chalk from 'chalk';
import { ConfigManager } from './lib/config-manager';
import { ConnectionManager } from './lib/connection-manager';

// 自定义选项处理函数
function parsePort(value: string): number {
  const parsedValue = parseInt(value, 10);
  if (isNaN(parsedValue) || parsedValue < 1 || parsedValue > 65535) {
    throw new InvalidArgumentError('端口号必须是 1-65535 之间的数字');
  }
  return parsedValue;
}

function parseMaxRows(value: string): number {
  const parsedValue = parseInt(value, 10);
  if (isNaN(parsedValue) || parsedValue < 1) {
    throw new InvalidArgumentError('行数必须是大于 0 的数字');
  }
  return parsedValue;
}

// 初始化管理器
const configManager = new ConfigManager();
const connectionManager = new ConnectionManager(configManager);

const program = new Command();

program
  .name('dm')
  .description(chalk.cyan('达梦数据库命令行工具 - 支持多连接管理'))
  .version('1.0.0', '-v, --version', '显示版本号')
  .option('-c, --connection <name>', '使用指定的连接别名')
  .option('-H, --host <host>', '数据库主机')
  .option('-p, --port <port>', '数据库端口', parsePort)
  .option('-u, --user <user>', '用户名')
  .option('-P, --password <password>', '密码')
  .option('-d, --database <database>', '数据库名')
  .option('-s, --schema <schema>', '默认 Schema')
  .option('--connect-string <dsn>', '连接字符串')
  .option('--format <format>', '输出格式 (table|json|csv|tsv)', 'table')
  .option('--max-rows <n>', '最大显示行数', parseMaxRows, 1000)
  .option('--verbose', '详细输出', false)
  .option('--no-color', '禁用颜色输出')
  .hook('preAction', async (thisCommand) => {
    await configManager.load();
  });

// 注册子命令
import { connectCommand } from './commands/connect';
import { queryCommand } from './commands/query';
import { execCommand } from './commands/exec';
import { exportCommand } from './commands/export';
import { connectionCommand } from './commands/connection';
import { configCommand } from './commands/config';
import { schemaCommand } from './commands/schema';
import { tableCommand } from './commands/table';
import { userCommand } from './commands/user';
import { dataCommand } from './commands/data';
import { performanceCommand } from './commands/performance';
import { maintenanceCommand } from './commands/maintenance';
import { shellCommand } from './commands/shell';

// 基础命令
program.addCommand(connectCommand(configManager, connectionManager));
program.addCommand(queryCommand(configManager, connectionManager));
program.addCommand(execCommand(configManager, connectionManager));
program.addCommand(exportCommand(configManager, connectionManager));
program.addCommand(connectionCommand(configManager));
program.addCommand(configCommand(configManager));

// 新增管理命令
program.addCommand(schemaCommand(configManager, connectionManager));
program.addCommand(tableCommand(configManager, connectionManager));
program.addCommand(userCommand(configManager, connectionManager));
program.addCommand(dataCommand(configManager, connectionManager));
program.addCommand(performanceCommand(configManager, connectionManager));
program.addCommand(maintenanceCommand(configManager, connectionManager));
program.addCommand(shellCommand(configManager, connectionManager));

// 使用 parseAsync 支持异步 action
program.parseAsync(process.argv).catch(async (error) => {
  console.error(chalk.red(error.message));
  if (program.opts().verbose) {
    console.error(chalk.gray(error.stack));
  }
  await connectionManager.closeAll();
  process.exit(1);
});
```

---

### 3.2 Schema 管理命令 (src/commands/schema.ts)

```typescript
import { Command } from 'commander';
import chalk from 'chalk';
import ora from 'ora';
import { input, confirm } from '@inquirer/prompts';
import { ConfigManager } from '../lib/config-manager';
import { ConnectionManager } from '../lib/connection-manager';
import { executeAndDisplay } from '../lib/formatter';

export function schemaCommand(
  configManager: ConfigManager,
  connectionManager: ConnectionManager
): Command {
  const cmd = new Command('schema')
    .description('Schema 管理');

  // 列出 Schema
  cmd
    .command('list')
    .alias('ls')
    .description('列出所有 Schema')
    .option('--user <user>', '显示指定用户的 Schema')
    .action(async (options) => {
      const sql = `
        SELECT 
          USERNAME AS "Owner",
          DEFAULT_TABLESPACE AS "Default Tablespace",
          CREATED AS "Created"
        FROM DBA_USERS
        ${options.user ? `WHERE USERNAME = '${options.user.toUpperCase()}'` : ''}
        ORDER BY USERNAME
      `;
      await executeAndDisplay(connectionManager, sql);
    });

  // 创建 Schema
  cmd
    .command('create <name>')
    .description('创建 Schema (用户)')
    .option('--password <password>', '用户密码')
    .option('--tablespace <ts>', '默认表空间')
    .action(async (name, options) => {
      const spinner = ora('创建 Schema...').start();

      try {
        // 如果没有指定密码，交互式输入
        let password = options.password;
        if (!password) {
          spinner.stop();
          password = await input({
            message: `请输入 ${name} 的密码:`,
            validate: (v) => v.length > 0 || '密码不能为空',
          });
        }

        let sql = `CREATE USER ${name} IDENTIFIED BY "${password}"`;
        if (options.tablespace) {
          sql += ` DEFAULT TABLESPACE ${options.tablespace}`;
        }

        await connectionManager.execute(sql);

        // 授予基本连接权限
        await connectionManager.execute(`GRANT CREATE SESSION TO ${name}`);

        spinner.succeed(chalk.green(`Schema "${name}" 创建成功`));
      } catch (error: any) {
        spinner.fail(chalk.red('创建失败'));
        console.error(chalk.red(error.message));
      }
    });

  // 删除 Schema
  cmd
    .command('drop <name>')
    .description('删除 Schema')
    .option('--cascade', '级联删除所有对象', false)
    .option('--force', '跳过确认', false)
    .action(async (name, options) => {
      if (!options.force) {
        const confirmed = await confirm({
          message: `确认删除 Schema "${name}"? ${options.cascade ? '(包含所有对象)' : ''}`,
          default: false,
        });
        if (!confirmed) return;
      }

      const spinner = ora('删除 Schema...').start();

      try {
        const sql = `DROP USER ${name} ${options.cascade ? 'CASCADE' : ''}`;
        await connectionManager.execute(sql);
        spinner.succeed(chalk.green(`Schema "${name}" 已删除`));
      } catch (error: any) {
        spinner.fail(chalk.red('删除失败'));
        console.error(chalk.red(error.message));
      }
    });

  // 切换 Schema
  cmd
    .command('use <name>')
    .description('切换当前 Schema')
    .action(async (name) => {
      try {
        await connectionManager.execute(`SET SCHEMA ${name}`);
        console.log(chalk.green(`已切换到 Schema "${name}"`));
      } catch (error: any) {
        console.error(chalk.red(error.message));
      }
    });

  // 查看 Schema 对象
  cmd
    .command('objects <name>')
    .description('查看 Schema 下的对象')
    .option('--type <type>', '对象类型 (table|view|sequence|procedure|all)', 'all')
    .action(async (name, options) => {
      const typeFilter = options.type === 'all' 
        ? '' 
        : `AND OBJECT_TYPE = '${options.type.toUpperCase()}'`;

      const sql = `
        SELECT 
          OBJECT_TYPE AS "Type",
          OBJECT_NAME AS "Name",
          CREATED AS "Created",
          STATUS AS "Status"
        FROM ALL_OBJECTS
        WHERE OWNER = '${name.toUpperCase()}'
        ${typeFilter}
        ORDER BY OBJECT_TYPE, OBJECT_NAME
      `;
      await executeAndDisplay(connectionManager, sql);
    });

  return cmd;
}
```

---

### 3.3 表管理命令 (src/commands/table.ts)

```typescript
import { Command } from 'commander';
import chalk from 'chalk';
import ora from 'ora';
import { input, select, confirm } from '@inquirer/prompts';
import { ConfigManager } from '../lib/config-manager';
import { ConnectionManager } from '../lib/connection-manager';
import { executeAndDisplay } from '../lib/formatter';

export function tableCommand(
  configManager: ConfigManager,
  connectionManager: ConnectionManager
): Command {
  const cmd = new Command('table')
    .description('表管理');

  // 列出表
  cmd
    .command('list')
    .alias('ls')
    .description('列出表')
    .option('-s, --schema <schema>', '指定 Schema')
    .option('-p, --pattern <pattern>', '名称匹配模式')
    .action(async (options) => {
      const schema = options.schema || await getCurrentSchema(connectionManager);
      const pattern = options.pattern ? `AND TABLE_NAME LIKE '${options.pattern}'` : '';

      const sql = `
        SELECT 
          TABLE_NAME AS "Table Name",
          NUM_ROWS AS "Rows",
          LAST_ANALYZED AS "Last Analyzed",
          TABLESPACE_NAME AS "Tablespace"
        FROM ALL_TABLES
        WHERE OWNER = '${schema.toUpperCase()}'
        ${pattern}
        ORDER BY TABLE_NAME
      `;
      await executeAndDisplay(connectionManager, sql);
    });

  // 查看表结构
  cmd
    .command('describe <table>')
    .alias('desc')
    .description('查看表结构')
    .option('-s, --schema <schema>', '指定 Schema')
    .action(async (table, options) => {
      const schema = options.schema || await getCurrentSchema(connectionManager);

      // 列信息
      console.log(chalk.cyan('\n列信息:'));
      const columnsSql = `
        SELECT 
          COLUMN_NAME AS "Column",
          DATA_TYPE AS "Type",
          DATA_LENGTH AS "Length",
          NULLABLE AS "Nullable",
          DATA_DEFAULT AS "Default"
        FROM ALL_TAB_COLUMNS
        WHERE OWNER = '${schema.toUpperCase()}'
          AND TABLE_NAME = '${table.toUpperCase()}'
        ORDER BY COLUMN_ID
      `;
      await executeAndDisplay(connectionManager, columnsSql);

      // 约束信息
      console.log(chalk.cyan('\n约束信息:'));
      const constraintsSql = `
        SELECT 
          CONSTRAINT_NAME AS "Constraint",
          CONSTRAINT_TYPE AS "Type",
          SEARCH_CONDITION AS "Condition"
        FROM ALL_CONSTRAINTS
        WHERE OWNER = '${schema.toUpperCase()}'
          AND TABLE_NAME = '${table.toUpperCase()}'
      `;
      await executeAndDisplay(connectionManager, constraintsSql);

      // 索引信息
      console.log(chalk.cyan('\n索引信息:'));
      const indexesSql = `
        SELECT 
          INDEX_NAME AS "Index",
          UNIQUENESS AS "Unique",
          COLUMN_NAME AS "Column"
        FROM ALL_IND_COLUMNS
        WHERE TABLE_OWNER = '${schema.toUpperCase()}'
          AND TABLE_NAME = '${table.toUpperCase()}'
        ORDER BY INDEX_NAME, COLUMN_POSITION
      `;
      await executeAndDisplay(connectionManager, indexesSql);
    });

  // 创建表（交互式）
  cmd
    .command('create <name>')
    .description('创建表')
    .option('-i, --interactive', '交互式创建', false)
    .option('-s, --schema <schema>', '指定 Schema')
    .action(async (name, options) => {
      if (options.interactive) {
        await interactiveCreateTable(connectionManager, name, options.schema);
      } else {
        console.log(chalk.yellow('请使用 --interactive 选项进行交互式创建'));
        console.log(chalk.gray('或直接使用 SQL: dm query "CREATE TABLE ..."'));
      }
    });

  // 删除表
  cmd
    .command('drop <name>')
    .description('删除表')
    .option('-s, --schema <schema>', '指定 Schema')
    .option('--cascade', '级联删除约束', false)
    .option('--purge', '彻底删除(不放入回收站)', false)
    .option('--force', '跳过确认', false)
    .action(async (name, options) => {
      if (!options.force) {
        const confirmed = await confirm({
          message: `确认删除表 "${name}"?`,
          default: false,
        });
        if (!confirmed) return;
      }

      const schema = options.schema || await getCurrentSchema(connectionManager);
      let sql = `DROP TABLE ${schema}.${name}`;
      if (options.cascade) sql += ' CASCADE CONSTRAINTS';
      if (options.purge) sql += ' PURGE';

      const spinner = ora('删除表...').start();

      try {
        await connectionManager.execute(sql);
        spinner.succeed(chalk.green(`表 "${name}" 已删除`));
      } catch (error: any) {
        spinner.fail(chalk.red('删除失败'));
        console.error(chalk.red(error.message));
      }
    });

  // 查看表数据
  cmd
    .command('data <table>')
    .description('查看表数据')
    .option('-s, --schema <schema>', '指定 Schema')
    .option('-l, --limit <n>', '限制行数', '100')
    .option('-w, --where <condition>', 'WHERE 条件')
    .option('-o, --order <columns>', '排序列')
    .option('-f, --format <format>', '输出格式', 'table')
    .action(async (table, options) => {
      const schema = options.schema || await getCurrentSchema(connectionManager);
      let sql = `SELECT * FROM ${schema}.${table}`;
      if (options.where) sql += ` WHERE ${options.where}`;
      if (options.order) sql += ` ORDER BY ${options.order}`;
      sql += ` FETCH FIRST ${options.limit} ROWS ONLY`;

      await executeAndDisplay(connectionManager, sql, options.format);
    });

  // 索引管理子命令
  const indexCmd = cmd
    .command('index')
    .description('索引管理');

  indexCmd
    .command('list <table>')
    .description('列出表的索引')
    .option('-s, --schema <schema>', '指定 Schema')
    .action(async (table, options) => {
      const schema = options.schema || await getCurrentSchema(connectionManager);
      const sql = `
        SELECT 
          i.INDEX_NAME AS "Index Name",
          i.UNIQUENESS AS "Unique",
          i.INDEX_TYPE AS "Type",
          LISTAGG(ic.COLUMN_NAME, ', ') WITHIN GROUP (ORDER BY ic.COLUMN_POSITION) AS "Columns",
          i.STATUS AS "Status"
        FROM ALL_INDEXES i
        JOIN ALL_IND_COLUMNS ic ON i.INDEX_NAME = ic.INDEX_NAME
          AND i.OWNER = ic.INDEX_OWNER
        WHERE i.TABLE_OWNER = '${schema.toUpperCase()}'
          AND i.TABLE_NAME = '${table.toUpperCase()}'
        GROUP BY i.INDEX_NAME, i.UNIQUENESS, i.INDEX_TYPE, i.STATUS
        ORDER BY i.INDEX_NAME
      `;
      await executeAndDisplay(connectionManager, sql);
    });

  indexCmd
    .command('create <table>')
    .description('创建索引')
    .option('-c, --columns <columns>', '索引列(逗号分隔)')
    .option('-u, --unique', '唯一索引', false)
    .option('-n, --name <name>', '索引名称')
    .option('-s, --schema <schema>', '指定 Schema')
    .action(async (table, options) => {
      if (!options.columns) {
        console.error(chalk.red('请指定索引列 --columns'));
        return;
      }

      const schema = options.schema || await getCurrentSchema(connectionManager);
      const indexName = options.name || `IDX_${table}_${options.columns.replace(/,/g, '_')}`;
      const unique = options.unique ? 'UNIQUE' : '';

      const sql = `CREATE ${unique} INDEX ${indexName} ON ${schema}.${table} (${options.columns})`;

      const spinner = ora('创建索引...').start();

      try {
        await connectionManager.execute(sql);
        spinner.succeed(chalk.green(`索引 "${indexName}" 创建成功`));
      } catch (error: any) {
        spinner.fail(chalk.red('创建失败'));
        console.error(chalk.red(error.message));
      }
    });

  return cmd;
}

/**
 * 交互式创建表
 */
async function interactiveCreateTable(
  connectionManager: ConnectionManager,
  tableName: string,
  schema?: string
): Promise<void> {
  console.log(chalk.cyan(`\n创建表 ${tableName}\n`));

  const columns: Array<{
    name: string;
    type: string;
    nullable: boolean;
    primaryKey: boolean;
    defaultValue?: string;
  }> = [];

  let addingColumns = true;

  while (addingColumns) {
    const columnName = await input({
      message: '列名 (输入空值结束):',
      validate: (v) => !v || /^[A-Za-z_]\w*$/.test(v) || '无效的列名',
    });

    if (!columnName) break;

    const dataType = await select({
      message: '数据类型:',
      choices: [
        { name: 'VARCHAR2(n)', value: 'VARCHAR2' },
        { name: 'NUMBER(p,s)', value: 'NUMBER' },
        { name: 'INTEGER', value: 'INTEGER' },
        { name: 'DATE', value: 'DATE' },
        { name: 'TIMESTAMP', value: 'TIMESTAMP' },
        { name: 'CLOB', value: 'CLOB' },
        { name: 'BLOB', value: 'BLOB' },
        { name: '自定义', value: 'CUSTOM' },
      ],
    });

    let fullType = dataType;
    if (dataType === 'VARCHAR2') {
      const length = await input({ message: '长度:', default: '255' });
      fullType = `VARCHAR2(${length})`;
    } else if (dataType === 'NUMBER') {
      const precision = await input({ message: '精度 (可选):' });
      if (precision) {
        const scale = await input({ message: '小数位数 (默认0):', default: '0' });
        fullType = `NUMBER(${precision},${scale})`;
      }
    } else if (dataType === 'CUSTOM') {
      fullType = await input({ message: '自定义类型:' });
    }

    const nullable = await confirm({ message: '允许为空?', default: true });
    const isPrimaryKey = await confirm({ message: '设为主键?', default: false });

    let defaultValue: string | undefined;
    if (!isPrimaryKey) {
      const useDefault = await confirm({ message: '设置默认值?', default: false });
      if (useDefault) {
        defaultValue = await input({ message: '默认值:' });
      }
    }

    columns.push({
      name: columnName,
      type: fullType,
      nullable,
      primaryKey: isPrimaryKey,
      defaultValue,
    });

    console.log(chalk.green(`列 "${columnName}" 已添加`));
  }

  if (columns.length === 0) {
    console.log(chalk.yellow('未添加任何列，已取消'));
    return;
  }

  // 生成 DDL
  const schemaPrefix = schema ? `${schema}.` : '';
  const columnDefs = columns.map((col) => {
    let def = `  ${col.name} ${col.type}`;
    if (!col.nullable) def += ' NOT NULL';
    if (col.defaultValue) def += ` DEFAULT ${col.defaultValue}`;
    return def;
  });

  const primaryKeys = columns.filter((c) => c.primaryKey).map((c) => c.name);
  if (primaryKeys.length > 0) {
    columnDefs.push(`  PRIMARY KEY (${primaryKeys.join(', ')})`);
  }

  const ddl = `CREATE TABLE ${schemaPrefix}${tableName} (\n${columnDefs.join(',\n')}\n)`;

  console.log(chalk.cyan('\n生成的 DDL:'));
  console.log(chalk.gray(ddl));

  const confirmed = await confirm({ message: '\n确认创建?', default: true });
  if (confirmed) {
    const spinner = ora('创建表...').start();
    try {
      await connectionManager.execute(ddl);
      spinner.succeed(chalk.green(`表 "${tableName}" 创建成功`));
    } catch (error: any) {
      spinner.fail(chalk.red('创建失败'));
      console.error(chalk.red(error.message));
    }
  }
}

/**
 * 获取当前 Schema
 */
async function getCurrentSchema(connectionManager: ConnectionManager): Promise<string> {
  const result = await connectionManager.query(
    "SELECT SYS_CONTEXT('USERENV','CURRENT_SCHEMA') AS SCHEMA FROM DUAL"
  );
  return result.rows[0]?.SCHEMA || 'SYSDBA';
}
```

---

### 3.4 数据导入导出命令 (src/commands/data.ts)

**使用 adaltas/node-csv 流式处理最佳实践**

```typescript
import { Command } from 'commander';
import chalk from 'chalk';
import ora from 'ora';
import fs from 'fs';
import path from 'path';
import { parse } from 'csv-parse';
import { stringify } from 'csv-stringify';
import { ConfigManager } from '../lib/config-manager';
import { ConnectionManager } from '../lib/connection-manager';

export function dataCommand(
  configManager: ConfigManager,
  connectionManager: ConnectionManager
): Command {
  const cmd = new Command('data')
    .description('数据导入导出');

  // ============ 导入子命令 ============
  const importCmd = cmd
    .command('import')
    .description('导入数据');

  // CSV 导入
  importCmd
    .command('csv <file>')
    .description('导入 CSV 文件')
    .requiredOption('-t, --table <table>', '目标表')
    .option('-s, --schema <schema>', '指定 Schema')
    .option('-d, --delimiter <char>', '分隔符', ',')
    .option('--header', '首行为表头', true)
    .option('--encoding <encoding>', '文件编码', 'utf-8')
    .option('--batch-size <n>', '批处理大小', '1000')
    .option('--skip-errors', '跳过错误行', false)
    .action(async (file, options) => {
      const schema = options.schema || await getCurrentSchema(connectionManager);
      const fullPath = path.resolve(file);

      if (!fs.existsSync(fullPath)) {
        console.error(chalk.red(`文件不存在: ${fullPath}`));
        return;
      }

      const spinner = ora('正在导入...').start();
      const startTime = Date.now();

      try {
        // 读取表结构获取列信息
        const columns = await getTableColumns(connectionManager, schema, options.table);
        
        // 创建 CSV 解析器（流式处理）
        const parser = fs.createReadStream(fullPath, { encoding: options.encoding })
          .pipe(parse({
            delimiter: options.delimiter,
            columns: options.header,
            skip_empty_lines: true,
            trim: true,
          }));

        let rowCount = 0;
        let errorCount = 0;
        const batch: any[] = [];
        const batchSize = parseInt(options.batchSize, 10);

        // 流式处理每一行
        for await (const record of parser) {
          try {
            const row = validateAndTransform(record, columns);
            batch.push(row);

            if (batch.length >= batchSize) {
              await insertBatch(connectionManager, schema, options.table, columns, batch);
              rowCount += batch.length;
              batch.length = 0;
              spinner.text = `已导入 ${rowCount} 行...`;
            }
          } catch (error: any) {
            errorCount++;
            if (!options.skipErrors) {
              throw error;
            }
            console.error(chalk.yellow(`\n行 ${rowCount + errorCount} 错误: ${error.message}`));
          }
        }

        // 插入剩余数据
        if (batch.length > 0) {
          await insertBatch(connectionManager, schema, options.table, columns, batch);
          rowCount += batch.length;
        }

        const duration = Date.now() - startTime;
        spinner.succeed(chalk.green(`导入完成: ${rowCount} 行 (${formatDuration(duration)})`));
        if (errorCount > 0) {
          console.log(chalk.yellow(`跳过 ${errorCount} 行错误`));
        }
      } catch (error: any) {
        spinner.fail(chalk.red('导入失败'));
        console.error(chalk.red(error.message));
      }
    });

  // JSON 导入
  importCmd
    .command('json <file>')
    .description('导入 JSON 文件')
    .requiredOption('-t, --table <table>', '目标表')
    .option('-s, --schema <schema>', '指定 Schema')
    .option('--array-path <path>', 'JSON 数组路径')
    .action(async (file, options) => {
      const schema = options.schema || await getCurrentSchema(connectionManager);
      const fullPath = path.resolve(file);

      const spinner = ora('正在导入...').start();

      try {
        const content = fs.readFileSync(fullPath, 'utf-8');
        let data = JSON.parse(content);

        // 支持嵌套路径
        if (options.arrayPath) {
          const paths = options.arrayPath.split('.');
          for (const p of paths) {
            data = data[p];
          }
        }

        if (!Array.isArray(data)) {
          throw new Error('JSON 数据必须是数组');
        }

        const columns = await getTableColumns(connectionManager, schema, options.table);
        let rowCount = 0;

        for (const record of data) {
          const row = validateAndTransform(record, columns);
          await insertRow(connectionManager, schema, options.table, columns, row);
          rowCount++;
          spinner.text = `已导入 ${rowCount} 行...`;
        }

        spinner.succeed(chalk.green(`导入完成: ${rowCount} 行`));
      } catch (error: any) {
        spinner.fail(chalk.red('导入失败'));
        console.error(chalk.red(error.message));
      }
    });

  // SQL 导入
  importCmd
    .command('sql <file>')
    .description('导入 SQL 文件')
    .option('--continue-on-error', '遇到错误继续执行', false)
    .option('--dry-run', '仅显示要执行的 SQL', false)
    .action(async (file, options) => {
      const fullPath = path.resolve(file);
      const content = fs.readFileSync(fullPath, 'utf-8');

      // 分割 SQL 语句
      const statements = content
        .split(/;\s*$/m)
        .map((s) => s.trim())
        .filter((s) => s && !s.startsWith('--'));

      const spinner = ora(`正在执行 ${statements.length} 条 SQL...`).start();
      let success = 0;
      let failed = 0;

      for (const sql of statements) {
        try {
          if (options.dryRun) {
            spinner.stop();
            console.log(chalk.gray(`\n${sql};`));
          } else {
            await connectionManager.execute(sql);
          }
          success++;
          spinner.text = `进度: ${success + failed}/${statements.length}`;
        } catch (error: any) {
          failed++;
          spinner.stop();
          console.error(chalk.red(`\nSQL 执行失败: ${error.message}`));
          console.error(chalk.gray(sql));
          if (!options.continueOnError) {
            break;
          }
          spinner.start();
        }
      }

      spinner.stop();
      console.log(chalk.green(`\n执行完成: ${success} 成功, ${failed} 失败`));
    });

  // ============ 导出子命令 ============
  const exportCmd = cmd
    .command('export')
    .description('导出数据');

  // CSV 导出
  exportCmd
    .command('csv <table>')
    .description('导出为 CSV')
    .requiredOption('-o, --output <file>', '输出文件')
    .option('-s, --schema <schema>', '指定 Schema')
    .option('-w, --where <condition>', 'WHERE 条件')
    .option('-c, --columns <columns>', '指定列(逗号分隔)', '*')
    .option('-d, --delimiter <char>', '分隔符', ',')
    .option('--no-header', '不包含表头')
    .action(async (table, options) => {
      const schema = options.schema || await getCurrentSchema(connectionManager);
      const outputPath = path.resolve(options.output);

      const spinner = ora('正在导出...').start();

      try {
        let sql = `SELECT ${options.columns} FROM ${schema}.${table}`;
        if (options.where) sql += ` WHERE ${options.where}`;

        const result = await connectionManager.query(sql);
        const columns = result.metaData?.map((m: any) => m.name) || Object.keys(result.rows[0] || {});

        // 使用 csv-stringify 流式写入
        const stringifier = stringify({
          header: options.header !== false,
          delimiter: options.delimiter,
        });

        const writeStream = fs.createWriteStream(outputPath);
        stringifier.pipe(writeStream);

        if (options.header !== false) {
          stringifier.write(columns);
        }

        for (const row of result.rows) {
          stringifier.write(columns.map((col) => row[col] ?? ''));
        }

        stringifier.end();

        await new Promise((resolve) => writeStream.on('finish', resolve));

        spinner.succeed(chalk.green(`导出完成: ${result.rows.length} 行 -> ${outputPath}`));
      } catch (error: any) {
        spinner.fail(chalk.red('导出失败'));
        console.error(chalk.red(error.message));
      }
    });

  // JSON 导出
  exportCmd
    .command('json <table>')
    .description('导出为 JSON')
    .requiredOption('-o, --output <file>', '输出文件')
    .option('-s, --schema <schema>', '指定 Schema')
    .option('-w, --where <condition>', 'WHERE 条件')
    .option('--pretty', '格式化输出', false)
    .action(async (table, options) => {
      const schema = options.schema || await getCurrentSchema(connectionManager);

      const spinner = ora('正在导出...').start();

      try {
        let sql = `SELECT * FROM ${schema}.${table}`;
        if (options.where) sql += ` WHERE ${options.where}`;

        const result = await connectionManager.query(sql);

        const jsonStr = options.pretty
          ? JSON.stringify(result.rows, null, 2)
          : JSON.stringify(result.rows);

        fs.writeFileSync(path.resolve(options.output), jsonStr, 'utf-8');

        spinner.succeed(chalk.green(`导出完成: ${result.rows.length} 行 -> ${options.output}`));
      } catch (error: any) {
        spinner.fail(chalk.red('导出失败'));
        console.error(chalk.red(error.message));
      }
    });

  // SQL 导出 (DDL)
  exportCmd
    .command('sql <table>')
    .description('导出 SQL (DDL)')
    .requiredOption('-o, --output <file>', '输出文件')
    .option('-s, --schema <schema>', '指定 Schema')
    .option('--data', '包含数据', false)
    .action(async (table, options) => {
      const schema = options.schema || await getCurrentSchema(connectionManager);

      const spinner = ora('正在导出...').start();

      try {
        // 获取表 DDL
        const ddlResult = await connectionManager.query(
          `SELECT DBMS_METADATA.GET_DDL('TABLE', '${table}', '${schema}') AS DDL FROM DUAL`
        );
        let output = ddlResult.rows[0]?.DDL + ';\n\n';

        // 如果需要数据
        if (options.data) {
          const result = await connectionManager.query(`SELECT * FROM ${schema}.${table}`);
          for (const row of result.rows) {
            const columns = Object.keys(row);
            const values = columns.map((col) => formatValue(row[col]));
            output += `INSERT INTO ${schema}.${table} (${columns.join(', ')}) VALUES (${values.join(', ')});\n`;
          }
        }

        fs.writeFileSync(path.resolve(options.output), output, 'utf-8');

        spinner.succeed(chalk.green(`导出完成 -> ${options.output}`));
      } catch (error: any) {
        spinner.fail(chalk.red('导出失败'));
        console.error(chalk.red(error.message));
      }
    });

  return cmd;
}

/**
 * 批量插入数据
 */
async function insertBatch(
  connectionManager: ConnectionManager,
  schema: string,
  table: string,
  columns: ColumnInfo[],
  rows: any[]
): Promise<void> {
  const columnNames = columns.map((c) => c.name).join(', ');
  const placeholders = columns.map((_, i) => `:${i + 1}`).join(', ');
  const sql = `INSERT INTO ${schema}.${table} (${columnNames}) VALUES (${placeholders})`;

  const bindRows = rows.map((row) =>
    columns.map((col) => row[col.name] ?? null)
  );

  await connectionManager.executeMany(sql, bindRows);
}

/**
 * 获取表列信息
 */
async function getTableColumns(
  connectionManager: ConnectionManager,
  schema: string,
  table: string
): Promise<ColumnInfo[]> {
  const result = await connectionManager.query(`
    SELECT 
      COLUMN_NAME,
      DATA_TYPE,
      DATA_LENGTH,
      DATA_PRECISION,
      DATA_SCALE,
      NULLABLE
    FROM ALL_TAB_COLUMNS
    WHERE OWNER = '${schema.toUpperCase()}'
      AND TABLE_NAME = '${table.toUpperCase()}'
    ORDER BY COLUMN_ID
  `);

  return result.rows.map((row: any) => ({
    name: row.COLUMN_NAME,
    type: row.DATA_TYPE,
    length: row.DATA_LENGTH,
    precision: row.DATA_PRECISION,
    scale: row.DATA_SCALE,
    nullable: row.NULLABLE === 'Y',
  }));
}

interface ColumnInfo {
  name: string;
  type: string;
  length: number;
  precision: number;
  scale: number;
  nullable: boolean;
}

/**
 * 验证和转换数据
 */
function validateAndTransform(record: any, columns: ColumnInfo[]): any {
  const transformed: any = {};
  
  for (const col of columns) {
    const value = record[col.name];
    
    if (value === undefined || value === null || value === '') {
      transformed[col.name] = col.nullable ? null : undefined;
      continue;
    }

    // 类型转换
    switch (col.type) {
      case 'NUMBER':
      case 'INTEGER':
      case 'FLOAT':
        transformed[col.name] = Number(value);
        if (isNaN(transformed[col.name])) {
          throw new Error(`列 "${col.name}" 的值 "${value}" 不是有效数字`);
        }
        break;
      case 'DATE':
      case 'TIMESTAMP':
        transformed[col.name] = new Date(value);
        if (isNaN(transformed[col.name].getTime())) {
          throw new Error(`列 "${col.name}" 的值 "${value}" 不是有效日期`);
        }
        break;
      default:
        transformed[col.name] = String(value);
    }
  }

  return transformed;
}

/**
 * 格式化值为 SQL 字面量
 */
function formatValue(value: any): string {
  if (value === null || value === undefined) {
    return 'NULL';
  }
  if (typeof value === 'number') {
    return String(value);
  }
  if (value instanceof Date) {
    return `TO_DATE('${value.toISOString().slice(0, 19).replace('T', ' ')}', 'YYYY-MM-DD HH24:MI:SS')`;
  }
  return `'${String(value).replace(/'/g, "''")}'`;
}

function formatDuration(ms: number): string {
  if (ms < 1000) return `${ms}ms`;
  return `${(ms / 1000).toFixed(2)}s`;
}
```

---

### 3.5 性能分析命令 (src/commands/performance.ts)

```typescript
import { Command } from 'commander';
import chalk from 'chalk';
import ora from 'ora';
import { ConfigManager } from '../lib/config-manager';
import { ConnectionManager } from '../lib/connection-manager';
import { executeAndDisplay, printTable } from '../lib/formatter';

export function performanceCommand(
  configManager: ConfigManager,
  connectionManager: ConnectionManager
): Command {
  const cmd = new Command('performance')
    .alias('perf')
    .description('性能分析');

  // 执行计划
  cmd
    .command('explain <sql>')
    .description('显示执行计划')
    .option('-f, --format <format>', '输出格式 (text|json)', 'text')
    .option('--analyze', '实际执行并统计', false)
    .action(async (sql, options) => {
      const spinner = ora('分析执行计划...').start();

      try {
        // 执行 EXPLAIN PLAN
        await connectionManager.execute(`EXPLAIN PLAN FOR ${sql}`);

        // 获取执行计划
        const planSql = `
          SELECT 
            OPERATION AS "Operation",
            OPTIONS AS "Options",
            OBJECT_NAME AS "Object",
            CARDINALITY AS "Rows",
            COST AS "Cost",
            TIME AS "Time"
          FROM PLAN_TABLE
          ORDER BY ID
        `;
        const result = await connectionManager.query(planSql);
        spinner.stop();

        if (options.format === 'json') {
          console.log(JSON.stringify(result.rows, null, 2));
        } else {
          console.log(chalk.cyan('\n执行计划:'));
          printTable(
            ['Operation', 'Options', 'Object', 'Rows', 'Cost', 'Time'],
            result.rows
          );
        }

        // 清理计划表
        await connectionManager.execute('DELETE FROM PLAN_TABLE').catch(() => {});
      } catch (error: any) {
        spinner.fail(chalk.red('分析失败'));
        console.error(chalk.red(error.message));
      }
    });

  // 慢查询分析
  cmd
    .command('slow')
    .description('慢查询分析')
    .option('--top <n>', '显示前 N 条', '20')
    .option('--export <file>', '导出到文件')
    .action(async (options) => {
      const spinner = ora('分析慢查询...').start();

      try {
        const sql = `
          SELECT 
            SQL_TEXT AS "SQL",
            EXECUTIONS AS "Executions",
            ELAPSED_TIME / 1000 AS "Total Time (ms)",
            ELAPSED_TIME / EXECUTIONS / 1000 AS "Avg Time (ms)",
            BUFFER_GETS AS "Buffer Gets",
            DISK_READS AS "Disk Reads",
            ROWS_PROCESSED AS "Rows"
          FROM V$SQL_STAT
          WHERE EXECUTIONS > 0
            AND ELAPSED_TIME / EXECUTIONS > 1000
          ORDER BY ELAPSED_TIME / EXECUTIONS DESC
          FETCH FIRST ${options.top} ROWS ONLY
        `;

        const result = await connectionManager.query(sql);
        spinner.stop();

        if (options.export) {
          const fs = require('fs');
          const jsonStr = JSON.stringify(result.rows, null, 2);
          fs.writeFileSync(options.export, jsonStr, 'utf-8');
          console.log(chalk.green(`已导出到 ${options.export}`));
        } else {
          console.log(chalk.cyan(`\n慢查询 Top ${options.top}:\n`));
          for (const row of result.rows) {
            console.log(chalk.yellow('─'.repeat(80)));
            console.log(chalk.gray(`SQL: ${row.SQL?.substring(0, 200)}...`));
            console.log(`执行次数: ${row.Executions}  平均耗时: ${row['Avg Time (ms)']}ms`);
            console.log(`缓冲区读取: ${row['Buffer Gets']}  磁盘读取: ${row['Disk Reads']}`);
          }
        }
      } catch (error: any) {
        spinner.fail(chalk.red('分析失败'));
        console.error(chalk.red(error.message));
      }
    });

  // 索引建议
  cmd
    .command('index-suggest <table>')
    .description('索引建议')
    .option('-s, --schema <schema>', '指定 Schema')
    .action(async (table, options) => {
      const schema = options.schema || await getCurrentSchema(connectionManager);
      const spinner = ora('分析索引...').start();

      try {
        // 检查现有索引
        const existingIndexesSql = `
          SELECT 
            INDEX_NAME,
            COLUMN_NAME,
            COLUMN_POSITION
          FROM ALL_IND_COLUMNS
          WHERE TABLE_OWNER = '${schema.toUpperCase()}'
            AND TABLE_NAME = '${table.toUpperCase()}'
          ORDER BY INDEX_NAME, COLUMN_POSITION
        `;
        const existingIndexes = await connectionManager.query(existingIndexesSql);

        // 分析查询模式
        const queryPatternsSql = `
          SELECT 
            SQL_TEXT,
            EXECUTIONS,
            ELAPSED_TIME
          FROM V$SQL_STAT
          WHERE SQL_TEXT LIKE '%${table.toUpperCase()}%'
            AND EXECUTIONS > 10
          ORDER BY ELAPSED_TIME DESC
          FETCH FIRST 50 ROWS ONLY
        `;
        const queries = await connectionManager.query(queryPatternsSql);

        spinner.stop();

        // 输出建议
        console.log(chalk.cyan('\n索引分析报告:\n'));

        console.log(chalk.yellow('现有索引:'));
        if (existingIndexes.rows.length === 0) {
          console.log(chalk.gray('  无'));
        } else {
          const indexMap = new Map<string, string[]>();
          for (const row of existingIndexes.rows) {
            const cols = indexMap.get(row.INDEX_NAME) || [];
            cols.push(row.COLUMN_NAME);
            indexMap.set(row.INDEX_NAME, cols);
          }
          for (const [name, cols] of indexMap) {
            console.log(chalk.gray(`  ${name}: ${cols.join(', ')}`));
          }
        }

        // 分析缺失索引
        const usedColumns = new Map<string, number>();
        for (const row of queries.rows) {
          const sqlText = row.SQL_TEXT?.toUpperCase() || '';
          const whereMatch = sqlText.match(/WHERE\s+(.+?)(?:ORDER|GROUP|LIMIT|$)/);
          if (whereMatch) {
            const conditions = whereMatch[1].split(/\s+AND\s+/);
            for (const cond of conditions) {
              const colMatch = cond.match(/(\w+)\s*[=<>\!]/);
              if (colMatch) {
                const col = colMatch[1];
                usedColumns.set(col, (usedColumns.get(col) || 0) + 1);
              }
            }
          }
        }

        const indexedColumns = new Set(
          existingIndexes.rows.map((r: any) => r.COLUMN_NAME)
        );

        const suggestions: string[] = [];
        for (const [col, usage] of usedColumns) {
          if (!indexedColumns.has(col) && usage > 5) {
            suggestions.push(`CREATE INDEX IDX_${table}_${col} ON ${schema}.${table} (${col});`);
          }
        }

        if (suggestions.length > 0) {
          console.log(chalk.green('\n建议创建的索引:'));
          for (const sql of suggestions) {
            console.log(chalk.green(`  ${sql}`));
          }
        } else {
          console.log(chalk.green('\n未发现缺失索引'));
        }
      } catch (error: any) {
        spinner.fail(chalk.red('分析失败'));
        console.error(chalk.red(error.message));
      }
    });

  // 统计信息
  cmd
    .command('stats <table>')
    .description('显示统计信息')
    .option('-s, --schema <schema>', '指定 Schema')
    .action(async (table, options) => {
      const schema = options.schema || await getCurrentSchema(connectionManager);

      console.log(chalk.cyan(`\n表 ${schema}.${table} 统计信息:\n`));

      // 表统计
      const tableStatsSql = `
        SELECT 
          NUM_ROWS AS "Rows",
          BLOCKS AS "Blocks",
          EMPTY_BLOCKS AS "Empty Blocks",
          AVG_ROW_LEN AS "Avg Row Length",
          LAST_ANALYZED AS "Last Analyzed"
        FROM ALL_TABLES
        WHERE OWNER = '${schema.toUpperCase()}'
          AND TABLE_NAME = '${table.toUpperCase()}'
      `;
      await executeAndDisplay(connectionManager, tableStatsSql);

      // 列统计
      console.log(chalk.cyan('\n列统计:'));
      const columnStatsSql = `
        SELECT 
          COLUMN_NAME AS "Column",
          NUM_DISTINCT AS "Distinct",
          NUM_NULLS AS "Nulls",
          AVG_COL_LEN AS "Avg Length",
          HISTOGRAM AS "Histogram"
        FROM ALL_TAB_COL_STATISTICS
        WHERE OWNER = '${schema.toUpperCase()}'
          AND TABLE_NAME = '${table.toUpperCase()}'
        ORDER BY COLUMN_NAME
      `;
      await executeAndDisplay(connectionManager, columnStatsSql);
    });

  return cmd;
}
```

---

### 3.6 交互式 Shell 增强 (src/interactive/shell.ts)

**使用 readline + 自动补全最佳实践**

```typescript
import * as readline from 'readline';
import chalk from 'chalk';
import { ConnectionManager } from '../lib/connection-manager';
import { ConfigManager } from '../lib/config-manager';
import { formatResult } from '../lib/formatter';

// Meta-Commands 定义
const META_COMMANDS: Record<string, {
  description: string;
  handler: (args: string[]) => Promise<string>;
}> = {
  '\\dn': {
    description: '列出所有 Schema',
    handler: async () => `
      SELECT USERNAME AS "Schema", DEFAULT_TABLESPACE AS "Tablespace", CREATED AS "Created"
      FROM DBA_USERS ORDER BY USERNAME
    `,
  },
  '\\dt': {
    description: '列出所有表',
    handler: async () => `
      SELECT TABLE_NAME AS "Table", NUM_ROWS AS "Rows", TABLESPACE_NAME AS "Tablespace"
      FROM USER_TABLES ORDER BY TABLE_NAME
    `,
  },
  '\\d': {
    description: '查看表结构 (\\d <table>)',
    handler: async (args) => {
      if (!args[0]) throw new Error('请指定表名: \\d <table>');
      return `
        SELECT 
          COLUMN_NAME AS "Column",
          DATA_TYPE AS "Type",
          DATA_LENGTH AS "Length",
          NULLABLE AS "Nullable",
          DATA_DEFAULT AS "Default"
        FROM USER_TAB_COLUMNS
        WHERE TABLE_NAME = '${args[0].toUpperCase()}'
        ORDER BY COLUMN_ID
      `;
    },
  },
  '\\di': {
    description: '列出所有索引',
    handler: async () => `
      SELECT 
        i.INDEX_NAME AS "Index",
        i.TABLE_NAME AS "Table",
        i.UNIQUENESS AS "Unique",
        LISTAGG(ic.COLUMN_NAME, ', ') WITHIN GROUP (ORDER BY ic.COLUMN_POSITION) AS "Columns"
      FROM USER_INDEXES i
      JOIN USER_IND_COLUMNS ic ON i.INDEX_NAME = ic.INDEX_NAME
      GROUP BY i.INDEX_NAME, i.TABLE_NAME, i.UNIQUENESS
      ORDER BY i.TABLE_NAME, i.INDEX_NAME
    `,
  },
  '\\dv': {
    description: '列出所有视图',
    handler: async () => `
      SELECT VIEW_NAME AS "View", TEXT_LENGTH AS "Text Length"
      FROM USER_VIEWS ORDER BY VIEW_NAME
    `,
  },
  '\\df': {
    description: '列出所有函数/过程',
    handler: async () => `
      SELECT OBJECT_NAME AS "Name", OBJECT_TYPE AS "Type", STATUS AS "Status"
      FROM USER_OBJECTS
      WHERE OBJECT_TYPE IN ('FUNCTION', 'PROCEDURE', 'PACKAGE')
      ORDER BY OBJECT_TYPE, OBJECT_NAME
    `,
  },
  '\\ds': {
    description: '列出所有序列',
    handler: async () => `
      SELECT SEQUENCE_NAME AS "Sequence", MIN_VALUE AS "Min", MAX_VALUE AS "Max", 
             INCREMENT_BY AS "Increment", CYCLE_FLAG AS "Cycle"
      FROM USER_SEQUENCES ORDER BY SEQUENCE_NAME
    `,
  },
  '\\du': {
    description: '列出所有用户',
    handler: async () => `
      SELECT USERNAME AS "User", ACCOUNT_STATUS AS "Status", 
             DEFAULT_TABLESPACE AS "Default TS", CREATED AS "Created"
      FROM DBA_USERS ORDER BY USERNAME
    `,
  },
  '\\conninfo': {
    description: '显示当前连接信息',
    handler: async () => 'CONNINFO',
  },
  '\\timing': {
    description: '开关执行时间显示',
    handler: async () => 'TIMING',
  },
  '\\x': {
    description: '开关扩展显示模式',
    handler: async () => 'EXPANDED',
  },
};

export async function runInteractiveShell(
  connectionManager: ConnectionManager,
  configManager: ConfigManager
): Promise<void> {
  // Shell 上下文
  const context = {
    timing: configManager.getCliConfig().showTiming ?? true,
    expanded: false,
    currentSchema: '',
  };

  // 自动补全函数
  const completer = async (line: string): Promise<[string[], string]> => {
    const words = line.trim().split(/\s+/);
    const lastWord = words[words.length - 1] || '';
    const prevWord = words[words.length - 2]?.toUpperCase() || '';

    let candidates: string[] = [];

    // Meta-commands 补全
    if (lastWord.startsWith('\\')) {
      candidates = Object.keys(META_COMMANDS).filter(cmd => cmd.startsWith(lastWord));
    }
    // SQL 关键字补全
    else if (['FROM', 'INTO', 'UPDATE', 'TABLE', 'JOIN'].includes(prevWord)) {
      try {
        const result = await connectionManager.query(
          "SELECT TABLE_NAME FROM USER_TABLES WHERE TABLE_NAME LIKE '" + lastWord.toUpperCase() + "%' ORDER BY TABLE_NAME"
        );
        candidates = result.rows.map((r: any) => r.TABLE_NAME);
      } catch {
        candidates = [];
      }
    }
    // 命令补全
    else {
      const commands = ['SELECT', 'INSERT', 'UPDATE', 'DELETE', 'CREATE', 'ALTER', 'DROP',
                        'FROM', 'WHERE', 'AND', 'OR', 'ORDER BY', 'GROUP BY', 'HAVING',
                        'JOIN', 'LEFT', 'RIGHT', 'INNER', 'OUTER', 'ON',
                        'help', 'exit', 'clear', 'history'];
      candidates = commands.filter(cmd => cmd.startsWith(lastWord.toUpperCase()));
    }

    return [candidates, lastWord];
  };

  // 创建 readline 接口
  const rl = readline.createInterface({
    input: process.stdin,
    output: process.stdout,
    prompt: getPrompt(context),
    historySize: 500,
    removeHistoryDuplicates: true,
    terminal: true,
  });

  // 设置自动补全
  (rl as any).completer = completer;

  // 显示欢迎信息
  console.log(chalk.cyan('达梦数据库交互式 Shell'));
  console.log(chalk.gray('输入 "help" 查看可用命令，输入 "exit" 退出'));
  console.log('');

  // 命令历史
  const history: string[] = [];

  // 获取提示符
  function getPrompt(ctx: typeof context): string {
    const connName = connectionManager.getCurrentName() || 'disconnected';
    const schema = ctx.currentSchema ? `/${ctx.currentSchema}` : '';
    return chalk.green(`dm [${connName}${schema}]> `);
  }

  // 处理输入
  rl.on('line', async (input: string) => {
    const trimmed = input.trim();
    
    if (!trimmed) {
      rl.prompt();
      return;
    }

    // 记录历史
    history.push(trimmed);

    try {
      // 处理 Meta-commands
      if (trimmed.startsWith('\\')) {
        await handleMetaCommand(trimmed);
      }
      // 处理普通命令
      else if (trimmed === 'help' || trimmed === '\\?') {
        showHelp();
      }
      else if (trimmed === 'exit' || trimmed === 'quit' || trimmed === '\\q') {
        console.log(chalk.gray('再见!'));
        rl.close();
        return;
      }
      else if (trimmed === 'clear' || trimmed === '\\c') {
        console.clear();
      }
      else if (trimmed === 'history') {
        showHistory();
      }
      // 执行 SQL
      else {
        await executeSQL(trimmed);
      }
    } catch (error: any) {
      console.error(chalk.red(error.message));
    }

    // 更新提示符
    rl.setPrompt(getPrompt(context));
    rl.prompt();
  });

  // 处理 Meta-commands
  async function handleMetaCommand(input: string): Promise<void> {
    const parts = input.split(/\s+/);
    const cmd = parts[0];
    const args = parts.slice(1);

    const metaCmd = META_COMMANDS[cmd];
    if (!metaCmd) {
      throw new Error(`未知命令: ${cmd}。输入 "\\?" 查看可用命令`);
    }

    const result = await metaCmd.handler(args);

    // 特殊命令处理
    if (result === 'CONNINFO') {
      const config = connectionManager.getCurrentConfig();
      if (config) {
        console.log(chalk.cyan('\n当前连接信息:'));
        console.log(`  连接名: ${connectionManager.getCurrentName()}`);
        console.log(`  主机: ${config.host}:${config.port}`);
        console.log(`  用户: ${config.user}`);
        if (config.database) console.log(`  数据库: ${config.database}`);
        if (config.schema) console.log(`  Schema: ${config.schema}`);
        console.log('');
      } else {
        console.log(chalk.yellow('未连接'));
      }
      return;
    }

    if (result === 'TIMING') {
      context.timing = !context.timing;
      console.log(chalk.gray(`执行时间显示: ${context.timing ? '开' : '关'}`));
      return;
    }

    if (result === 'EXPANDED') {
      context.expanded = !context.expanded;
      console.log(chalk.gray(`扩展显示: ${context.expanded ? '开' : '关'}`));
      return;
    }

    // 执行 SQL
    await executeSQL(result);
  }

  // 执行 SQL
  async function executeSQL(sql: string): Promise<void> {
    const startTime = Date.now();

    const result = await connectionManager.query(sql);

    const duration = Date.now() - startTime;

    if (result.rows && result.rows.length > 0) {
      const columns = result.metaData?.map((m: any) => m.name) || Object.keys(result.rows[0]);
      const output = formatResult(columns, result.rows, {
        format: context.expanded ? 'vertical' : 'table',
      });
      console.log(output);
    } else if (result.rowsAffected !== undefined) {
      console.log(chalk.green(`影响 ${result.rowsAffected} 行`));
    }

    if (context.timing) {
      console.log(chalk.gray(`执行时间: ${duration}ms`));
    }
  }

  // 显示帮助
  function showHelp(): void {
    console.log(chalk.cyan('\n可用命令:'));
    console.log('');
    console.log(chalk.yellow('Meta-commands:'));
    for (const [cmd, meta] of Object.entries(META_COMMANDS)) {
      console.log(`  ${cmd.padEnd(15)} ${meta.description}`);
    }
    console.log('');
    console.log(chalk.yellow('其他命令:'));
    console.log('  help, \\?       显示此帮助');
    console.log('  history        显示命令历史');
    console.log('  clear, \\c      清屏');
    console.log('  exit, quit, \\q 退出');
    console.log('');
    console.log(chalk.yellow('SQL:'));
    console.log('  直接输入 SQL 语句执行');
    console.log('');
  }

  // 显示历史
  function showHistory(): void {
    console.log(chalk.cyan('\n命令历史:'));
    const recent = history.slice(-20);
    recent.forEach((cmd, i) => {
      console.log(chalk.gray(`  ${(history.length - recent.length + i + 1).toString().padStart(4)}  ${cmd}`));
    });
    console.log('');
  }

  // 信号处理
  rl.on('SIGINT', () => {
    console.log(chalk.gray('\n输入 "exit" 退出'));
    rl.prompt();
  });

  rl.on('close', async () => {
    await connectionManager.closeAll();
    process.exit(0);
  });

  // 开始提示
  rl.prompt();
}
```

---

## 四、实现优先级

### Phase 1 (核心功能 - 1周)

| 功能 | 文件 | 工作量 |
|------|------|--------|
| Schema 管理 | `src/commands/schema.ts` | 2天 |
| 表列表/描述 | `src/commands/table.ts` (部分) | 1天 |
| 用户列表 | `src/commands/user.ts` (部分) | 1天 |
| CSV 导出增强 | `src/commands/data.ts` (部分) | 1天 |

### Phase 2 (管理功能 - 2周)

| 功能 | 文件 | 工作量 |
|------|------|--------|
| 表创建(交互式) | `src/commands/table.ts` (完整) | 3天 |
| 用户管理 | `src/commands/user.ts` (完整) | 3天 |
| CSV/JSON 导入导出 | `src/commands/data.ts` (完整) | 3天 |
| 执行计划 | `src/commands/performance.ts` (部分) | 2天 |

### Phase 3 (高级功能 - 2周)

| 功能 | 文件 | 工作量 |
|------|------|--------|
| 索引建议 | `src/commands/performance.ts` (完整) | 3天 |
| 表空间管理 | `src/commands/maintenance.ts` (部分) | 2天 |
| 碎片整理 | `src/commands/maintenance.ts` (完整) | 2天 |
| 交互式 Shell 增强 | `src/interactive/shell.ts` (完整) | 3天 |

---

## 五、技术要点

### 5.1 Commander.js 嵌套子命令

```typescript
// 一级命令
const tableCmd = program.command('table').description('表管理');

// 二级命令
tableCmd.command('list').description('列出表');
tableCmd.command('describe <table>').description('查看表结构');

// 三级命令
const indexCmd = tableCmd.command('index').description('索引管理');
indexCmd.command('list <table>').description('列出索引');
indexCmd.command('create <table>').description('创建索引');
```

### 5.2 readline 自动补全

```typescript
const completer = async (line: string): Promise<[string[], string]> => {
  const words = line.trim().split(/\s+/);
  const lastWord = words[words.length - 1] || '';
  const prevWord = words[words.length - 2]?.toUpperCase() || '';

  let candidates: string[] = [];

  // 根据上下文提供补全
  if (lastWord.startsWith('\\')) {
    candidates = Object.keys(META_COMMANDS).filter(cmd => cmd.startsWith(lastWord));
  } else if (['FROM', 'TABLE'].includes(prevWord)) {
    candidates = await getTableSuggestions(lastWord);
  }

  return [candidates, lastWord];
};
```

### 5.3 CSV 流式处理

```typescript
// 流式解析（处理大文件）
const parser = fs.createReadStream(file)
  .pipe(parse({ columns: true, skip_empty_lines: true }));

for await (const record of parser) {
  // 逐行处理，内存占用低
}

// 流式写入
const stringifier = stringify({ header: true });
stringifier.pipe(fs.createWriteStream(output));
stringifier.write(columns);
stringifier.end();
```

---

*文档生成日期: 2026-06-15*
*基于 Commander.js、readline、csv-parse/stringify 官方文档最佳实践*
